<div id="refer">
  <p class="congrats">Congrats! You're entered into the sweepstakes!</p>
  <div class="intro">
    <h1>Refer a friend</h1>
    <p>Earn $10 in credit for each friend who signs up, with a cap of $200 in total credit.  Copy and paste the link and email it to your friends or share on social:</p>
  </div>
  <div class="link">
    <%= text_field :user, :link, :value => root_url + '?ref=' + @user.referral_code %>
    <p>Bookmark this page and check back to see how many credits you’ve earned.</p>
    <a class="share">Share</a>
    <ul class="share-links">
      <% # TODO: replace this insanity %>
      <% message = "Hey friends! Sign up for Bill Blass's private launch offer, and get entered to win $3000 of product." %>
      <% email_title = "Somebody Chic Invites You to billblass.com" %>
      <% email_content = %(A fabulous friend has invited you to the exclusive pre-launch of Bill Blass. When you sign up for the private launch offer, you will be entered to win $3000 of Bill Blass product.%0D%0A%0D%0AEven better, for every stylish friend you refer, you will receive a $10 credit if they register, up to $200!%0D%0A%0D%0ASign up here:) %>
      <li class="twitter"><a target="_blank" href="https://twitter.com/intent/tweet?text=<%= CGI::escape(message + " " + root_url + '?ref=' + @user.referral_code) %>"><i class="fa fa-twitter"></i></a></li>
      <li class="facebook"><a target="_blank" href="http://www.facebook.com/sharer/sharer.php?u=<%= CGI::escape(root_url + '?ref=' + @user.referral_code) %>&title=Bill+Blass"><i class="fa fa-facebook"></i></a></li>
      <li class="envelope"><a href="mailto:?subject=<%= email_title %>&body=<%= email_content + " " + root_url + '?ref=' + @user.referral_code %>"<i class="fa fa-envelope"></i></a></li>
    </ul>
  </div>
  <div class="rewards">
    Your rewards
    <div id="number-line">
      <ul class="numbers">
        <% (0..20).each do |n| %>
          <% if n == [@user.referrals.count, 20].min then %>
            <li class="current">$<%= n * 10 %></li>
          <% else %>
            <li>$<%= n * 10 %></li>
          <% end %>
        <% end %>
      </ul>
      <ul class="line">
        <% (0..20).each do |n| %>
          <% if n <= @user.referrals.count %>
            <li class="active">
              <div class="line-left"></div>
              <div class="line-middle"></div>
              <div class="line-right"></div>
            </li>
          <% else %>
            <li>
              <div class="line-left"></div>
              <div class="line-middle"></div>
              <div class="line-right"></div>
            </li>
          <% end %>
        <% end %>
      </ul>
      <div class="scale">
        <div class="scale-min">0 referrals</div>
        <div class="scale-max">20 referrals</div>
      </div>
    </div>
  </div>
</div>
